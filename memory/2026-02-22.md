# 2026-02-22 Project Discussion Log

## Discussion Notes
- Made LLM model names configurable instead of hardcoded
- Three models affected: chat model (intent classifier + RAG engine), embedding model (embedder)
- Added `OPENAI_CHAT_MODEL` and `OPENAI_EMBEDDING_MODEL` to Settings, .env, and .env.example files
- Updated README.md tech stack table and quick start section

## Conclusions & Decisions
- ✅ LLM model names are now environment-variable driven via `OPENAI_CHAT_MODEL` and `OPENAI_EMBEDDING_MODEL`
- ✅ Defaults remain `gpt-3.5-turbo` and `text-embedding-3-small` for backward compatibility
- ✅ Created agent delegation workflow rule (`.cursor/rules/agent-delegation-workflow.mdc`)

## Open Questions
(None)

## Related Tasks
- backend/STATUS.md
